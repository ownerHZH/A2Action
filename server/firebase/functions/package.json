{
  "name": "a2action-functions-demo",
  "private": true,
  "type": "module",
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "clean": "rimraf lib",
    "build:tool-vectors": "node scripts/build_tool_vectors.mjs",
    "copy:assets": "node scripts/copy_assets.mjs",
    "build": "npm run clean && tsc --project tsconfig.json && npm run copy:assets",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "npm run build:tool-vectors && npm run build && firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.6.0",
    "firebase-functions": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.19.25",
    "rimraf": "^6.1.0",
    "typescript": "^5.9.3"
  }
}
